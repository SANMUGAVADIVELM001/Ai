AI_PROVIDER=gemini
AI_MODEL=gemini-3.6-flash
GEMINI_API_KEY=
MONGODB_URI=
# Comma-separated list of allowed frontend origins (e.g. Vercel prod + preview URLs)
FRONTEND_ORIGIN=http://localhost:5180
# Set to "production" on Render — disables /api/dev and enables Secure cookies
NODE_ENV=development
